import { CardNumberDirective } from './card-number.directive';

describe('CardNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new CardNumberDirective();
    expect(directive).toBeTruthy();
  });
});
