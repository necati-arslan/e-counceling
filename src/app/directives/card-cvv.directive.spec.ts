import { CardCvvDirective } from './card-cvv.directive';

describe('CardCvvDirective', () => {
  it('should create an instance', () => {
    const directive = new CardCvvDirective();
    expect(directive).toBeTruthy();
  });
});
